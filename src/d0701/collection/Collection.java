package d0701.collection;

public class Collection {

    // 자바 컬렉션
    // 목록성 데이터를 처리하는 자료 구조

    // 자료구조 (Data Structure)
    // 어떤 정보를 담는 것을 의미
    // 하나의 데이터가 아닌 여러개의 데이터를 담을 때 사용한다

    // 분류
    // 순서가 있는 목록 형 (List)
    // 순서가 중요하지 않은 (Set)
    // 먼저 들어온 것이 먼저 나가는 큐 (Queue)
    // 키 - 값 (Key - Value) 으로 저장되는 (Map)

}
